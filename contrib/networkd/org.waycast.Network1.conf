<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
  <policy user="root">
    <allow own="org.waycast.Network1"/>
  </policy>
  <policy context="default">
    <!-- Methods perform Polkit and unique-sender session ownership checks. -->
    <allow send_destination="org.waycast.Network1"/>
  </policy>
</busconfig>
